import React from 'react';
import resume from './../../assets/resume.png'


function Resume () {
    return (
        <div style={{marginBottom: '20px'}}>
        <img src={resume} alt="Whitney Young Resume" style={{width: '60%', border: 'none'}}/>
        </div>
    )
}

export default Resume;
